# Copy to .env then edit values before starting the server
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/smartbeehive
JWT_SECRET=super_secret_change_me
PORT=5000
# Set your deployed frontend URL (e.g., https://smart-hive.vercel.app) to restrict CORS
CORS_ORIGIN=http://localhost:3000
